# ─── Wallet ───────────────────────────────────────────────────────────────────
# Your funded wallet on Base. On first boot, this key spawns the agent's own
# wallet (via skills.wallet()), funds it, then is never used again.
# The agent operates on its own key stored in data/wallet.json.
PINION_PRIVATE_KEY=0x...

# ─── Genesis seed amounts (optional, defaults shown) ─────────────────────────
# SEED_USDC=5                    # USDC to fund agent wallet
# SEED_ETH=0.001                 # ETH for gas fees

# ─── Network ──────────────────────────────────────────────────────────────────
# base          → mainnet
# base-sepolia  → testnet (dev only)
PINION_NETWORK=base

# ─── Optional PinionOS overrides ─────────────────────────────────────────────
# PINION_API_KEY=pk_...          # Unlimited key — skip x402 per-call charges
# BIRDEYE_API_KEY=...            # Birdeye token prices (used by skills.price)
# ONEINCH_API_KEY=...            # 1inch routing (used by skills.trade)
# PINION_API_URL=https://pinionos.com/skill
# FACILITATOR_URL=https://facilitator.payai.network
