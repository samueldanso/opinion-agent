# ─── Agent wallet ────────────────────────────────────────────────────────────
# Hex private key (0x...) — must have USDC on Base (or Base Sepolia for dev)
# This wallet PAYS for PinionOS skills AND RECEIVES earnings from predictions
PINION_PRIVATE_KEY=0x...

# Same address as above — passed to createSkillServer as payTo
ADDRESS=

# ─── Network ──────────────────────────────────────────────────────────────────
# base          → mainnet (demo)
# base-sepolia  → testnet (dev)
PINION_NETWORK=base-sepolia

# ─── Servers ──────────────────────────────────────────────────────────────────
# Port for the x402 prediction skill server (callers pay $0.10 USDC here)
SKILL_SERVER_PORT=4020

# Port for the SSE + dashboard API server
API_SERVER_PORT=3001

# ─── Frontend (Vercel) ────────────────────────────────────────────────────────
# Set in frontend/.env.local
# NEXT_PUBLIC_AGENT_URL=https://your-railway-url.up.railway.app
